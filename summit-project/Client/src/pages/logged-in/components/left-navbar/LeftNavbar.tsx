import { memo, ReactNode } from "react";
import './LeftNavbar.css';

function LeftNavbar(): ReactNode{
    return(
        <div className='leftNavbarCotainer'>
        </div>
    );
}

export default memo(LeftNavbar);